const Router = require('express')

const router = Router

router.post('/sign-up', )
router.post('/log-in', )
router.post('/log-out', )

router.get('/refresh', )
router.get('/users', )

modele.exports = router
